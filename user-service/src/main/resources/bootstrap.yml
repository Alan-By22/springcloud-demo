#nacos的配置
spring:
  application:
    name: userservice  #服务的名称
  profiles:
    active: dev #开发环境
  cloud:
    nacos:
      server-addr: localhost:80 #nacos的地址
      config:
        file-extension: yaml #文件的后缀名称